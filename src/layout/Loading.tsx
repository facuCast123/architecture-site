const Loading = () => {
  return (
    <div className="h-screen flex flex-col items-center justify-center">
      <h1 className="w-fit text-xl">Loading...</h1>
    </div>
  );
};

export default Loading;
